# ================================================================================
#                    EduCRM React Admin Dashboard
#                    Environment Configuration
# ================================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env in the same directory
# 2. Replace placeholder values with your actual configuration
# 3. NEVER commit your .env file to version control
# 4. Restart the development server after making changes
#
# ================================================================================

# ================================================================================
# API Configuration (REQUIRED)
# ================================================================================

# Base URL for your backend API
# This should point to your Node.js/Express backend server
# Examples:
#   Development: http://localhost:3009/api/
#   Production:  https://api.yourdomain.com/api/
VITE_API_BASE_URL=https://your-api-url.com/api/

# Socket.io URL for real-time features (chat, notifications)
# Usually the same as your backend server without the /api path
# Examples:
#   Development: http://localhost:3009
#   Production:  https://api.yourdomain.com
VITE_SOCKET_URL=https://your-socket-url.com

# ================================================================================
# Firebase Configuration (OPTIONAL - for Google Sign-In & Push Notifications)
# ================================================================================

# Get these values from Firebase Console:
# 1. Go to https://console.firebase.google.com/
# 2. Select your project (or create a new one)
# 3. Go to Project Settings > General
# 4. Scroll down to "Your apps" section
# 5. Click on the web app or add a new web app
# 6. Copy the configuration values

VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# VAPID Key for Web Push Notifications
# Get from Firebase Console:
# 1. Go to Project Settings > Cloud Messaging
# 2. Scroll to "Web configuration"
# 3. Generate or copy the key pair
VITE_FIREBASE_VAPID_KEY=your-vapid-key

# ================================================================================
# Application Configuration (OPTIONAL)
# ================================================================================

# Application display name
VITE_APP_NAME=EduCRM

# Application version (shown in UI)
VITE_APP_VERSION=1.0.0

# Feature flags
VITE_ENABLE_NOTIFICATIONS=true
VITE_ENABLE_CHAT=true

# ================================================================================
# IMPORTANT NOTES
# ================================================================================
#
# 1. All environment variables must start with VITE_ to be accessible in the app
#
# 2. These are FRONTEND environment variables only:
#    - They are embedded in the built JavaScript
#    - Do NOT store sensitive backend secrets here
#    - API keys for client-side services (Firebase) are OK
#
# 3. For production deployment:
#    - Set these variables in your hosting provider's dashboard
#    - Vercel: Settings > Environment Variables
#    - Netlify: Site settings > Build & deploy > Environment
#    - Railway: Variables tab
#
# 4. Backend API requirements:
#    - Your backend must implement the expected API endpoints
#    - See docs/API_INTEGRATION.md for endpoint specifications
#    - CORS must be configured to allow your frontend domain
#
# ================================================================================
